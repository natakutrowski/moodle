# Commerce 7.95F9 — Stabilisation de la suite de tests

## Objectif

Aligner les tests historiques F6–F8 avec l’architecture finale certifiée, sans modifier le code métier.

## Ajustements

- adaptation des tests de `CommerceCertificationReport` à son API actuelle par phase, inventaire et issues ;
- injection explicite des dépendances de `CommerceProductionReadinessAuditor` ;
- locale française explicite dans la fixture de l’éditeur Storefront ;
- alignement des assertions Storefront avec le renderer final F6I :
  - badge de type rendu par la carte ou le template de page ;
  - badges commerciaux rendus par `product_badges.mustache` ;
  - pages raccordées au renderer partagé via `product_commerce_panel.mustache` ;
  - hauteur des badges fixée à `1.75rem` ;
  - médaillon Gustave fixé à `4.0rem`.

## Portée

Cette livraison ne contient :

- aucune modification métier ;
- aucune migration de base de données ;
- aucun bump de version ;
- aucune modification du CSS canonique.

## Certification

```bash
vendor/bin/phpunit --testsuite local_subscriptions_testsuite
```
