# 7.95F6C — Visual polish, recommendations and findings

F6C completes Storefront merchandising and folds in the findings from F6A/F6B.

## Corrections
- automatic immutable SKU generated from the English technical name;
- CRM link between a Native course product and an existing Legacy plan/access scope;
- one valid active Native currency is sufficient for sellability;
- removal of the duplicate SQL `LIMIT` produced by `record_exists_sql()`;
- namespaced `core_text` resolution fixed.

## Polish
- badge icons with a giraffe fallback for `gustave_choice`;
- refined card hover, spacing and mobile rendering;
- explicit Native SKU recommendations, capped at four and hiding owned products.

The future custom Gustave icon can replace the giraffe fallback without changing the metadata contract.
